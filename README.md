# Boilerplate for small php CLI application

## Usage

Application can be run by typing `bin/console`.

## Customization

Everything is configured in a Pimple container that you can
found in the `src/Container` folder.
